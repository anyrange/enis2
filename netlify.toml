[build.environment]
  NPM_FLAGS = "--version" # prevents Netlify npm install
  NODE_VERSION = "16"

[build]
  command = "npx pnpm install --store=node_modules/.pnpm-store && npm run build"
  publish = "apps/web-mars/dist"

[[redirects]]
  from = "/*"
  to = "/"
  status = 200